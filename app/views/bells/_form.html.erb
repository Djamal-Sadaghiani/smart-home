<%= form_with(model: bell, local: true) do |form| %>
  <% if bell.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bell.errors.count, "error") %> prohibited this bell from being saved:</h2>

      <ul>
        <% bell.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
